// Google Apps Script で「ウェブアプリ」として導入（デプロイ）した際に発行されるURLを貼り付けてください。
// 手順はREADMEを参照してください。
//
// このURLは生徒ページから直接呼び出される公開エンドポイントです（生徒はログインしないため）。
// アクセス制御は秘密のURLであることではなく、Apps Script側のコードで
// 「渡された生徒IDに該当するデータだけを返す／試合結果の追記だけを許可する」よう行っています。
// そのためリポジトリにコミットして問題ありません。詳しくはREADMEを参照してください。
export const APPS_SCRIPT_URL = "";
